// Public deployment configuration only.
// There is deliberately no GitHub token in this file.
// Netlify Functions keep GITHUB_TOKEN private in the Netlify environment.
window.SNAPLINE_CONFIG = {
  owner: 'stakewindows63',
  repo: 'file',
  branch: 'main',
  folder: ''
};
